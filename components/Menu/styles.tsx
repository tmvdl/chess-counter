import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
  },
  title: {
    padding: 10,
    fontSize: 40,
    textAlign: 'center',
    fontWeight: 'bold',
    marginTop: 25,
  }
})
